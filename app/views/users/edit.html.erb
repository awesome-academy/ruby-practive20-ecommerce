<% provide(:title, t("users.edit.title")) %>
<h1><%= t("users.edit.title") %></h1>
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <%= render "form", form_url: user_path(@user), submit_label: t("users.edit.save_changes") %>
    </div>
  </div>
